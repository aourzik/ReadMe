[build]
builder = "dockerfile"
dockerfilePath = "Dockerfile"

[deploy]
startCommand = "sh -c 'bunx prisma db push && bun run src/index.ts'"
healthcheckPath = "/health"
healthcheckTimeout = 30
restartPolicyType = "on_failure"
